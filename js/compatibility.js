/**
 * nameorigin.io — compatibility.js
 * Phonetic Compatibility: rule-based scoring, bars, first+last analysis.
 * Logic lives in app.js; uses /data/compatibility_patterns.json when needed.
 */
(function () {
  'use strict';
  window.nameorigin = window.nameorigin || {};
  window.nameorigin.compatibility = { init: function () {} };
})();
